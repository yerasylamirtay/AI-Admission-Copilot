'use client';
import { Button } from '@/components/ui/Button'; import { Card } from '@/components/ui/Card'; import { Profile } from '@/lib/types';
const SCHOOLS = ['Назарбаев Интеллектуальная школа','Республиканская физико-математическая школа','БИЛ','Haileybury Almaty','Miras International School','Koldau Lyceum'];
export default function ProfileSetup({ profile, onUpdate, onNext }: { profile: Partial<Profile> & { name?: string; school?: string; city?: string }; onUpdate: (x: Partial<Profile>) => void; onNext: () => void }) { return <div className="max-w-2xl mx-auto space-y-6"><h2 className="text-heading-md">Базовые данные</h2><Card className="p-6 space-y-5"><input className="input-field" placeholder="Имя" value={profile.name ?? ''} onChange={e => onUpdate({ name: e.target.value } as any)} /><div className="grid md:grid-cols-2 gap-4"><select className="input-field" value={profile.grade ?? ''} onChange={e => onUpdate({ grade: Number(e.target.value) as any })}><option value="">Класс</option><option value="9">9 класс</option><option value="10">10 класс</option><option value="11">11 класс</option></select><input className="input-field" placeholder="Город" value={profile.city ?? ''} onChange={e => onUpdate({ city: e.target.value } as any)} /></div><input list="top-schools" className="input-field" placeholder="Школа" value={profile.school ?? ''} onChange={e => onUpdate({ school: e.target.value } as any)} /><datalist id="top-schools">{SCHOOLS.map(s => <option key={s} value={s} />)}</datalist><select className="input-field" value={profile.schoolType ?? 'general'} onChange={e => onUpdate({ schoolType: e.target.value as any })}><option value="general">Общеобразовательная школа</option><option value="nis">НИШ</option><option value="bil">БИЛ</option><option value="lyceum">Лицей</option><option value="gymnasium">Гимназия</option><option value="other">Другое</option></select><Button onClick={onNext}>Далее — учёба и тесты</Button></Card></div>; }
