'use client';
import { Button } from '@/components/ui/Button'; import { Card } from '@/components/ui/Card'; import { ChipGroup } from '@/components/ui/ChipGroup'; import { Profile } from '@/lib/types';
export default function Preferences({ profile, onUpdate, onComplete }: { profile: Partial<Profile>; onUpdate: (x: Partial<Profile>) => void; onComplete: () => void }) { return <div className="max-w-2xl mx-auto space-y-6"><h2 className="text-heading-md">Предпочтения</h2><Card className="p-6 space-y-6"><div><label className="block text-body-sm mb-2">Направления</label><ChipGroup multiple options={['Computer Science','Engineering','Business','Medicine','Law','Economics','Data Science','Design'].map(x => ({ value: x, label: x }))} selected={profile.specialties ?? []} onChange={(x: string[]) => onUpdate({ specialties: x })} /></div><div><label className="block text-body-sm mb-2">Регионы</label><ChipGroup multiple options={[['kazakhstan','🇰🇿 Казахстан'],['europe','🇪🇺 Европа'],['asia','🌏 Азия'],['usa','🇺🇸 США']].map(([value,label]) => ({ value, label }))} selected={profile.regions ?? []} onChange={(x: string[]) => onUpdate({ regions: x as any })} /></div><div><label className="block text-body-sm mb-2">Бюджет</label><ChipGroup options={[['grant','Грант / $0'],['5k','$5,000'],['15k','$15,000'],['25k+','$25,000+']].map(([value,label]) => ({ value, label }))} selected={profile.budget ?? ''} onChange={(x: string) => onUpdate({ budget: x as any })} /></div><Button onClick={onComplete}>Завершить онбординг →</Button></Card></div>; }
